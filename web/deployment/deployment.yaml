apiVersion: apps/v1
kind: Deployment
metadata:
  name: home-manager
  namespace: home-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: home-manager
  template:
    metadata:
      labels:
        app: home-manager
    spec:
      containers:
        - name: home-manager
          image: zhijietan97/home-manager:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
